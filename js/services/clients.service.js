export const ClientsService = {
    // Obtener todos los clientes (Próximamente desde Firebase)
    getAll: async () => {
        return []; 
    },

    // Crear nuevo cliente
    create: async (data) => {
        return data;
    },

    // Obtener cliente por ID
    getById: async (id) => {
        return null;
    },

    // Actualizar cliente
    update: async (id, data) => {
        return data;
    }
};